#простая сборка, состоит из одного файла

#программа выполняет вывод в консоль прредложения

#команды консоли для генерации и билда файлов проекта
#все действия в консоли начинаются с папки example1

mkdir build
cd build
cmake .. //генерация файлов проекта для IDE по умолчанию в каталоге build, если нужноо выбрать генератор, то cmake -G "название генератора".
# список генераторов можно глянуть cmake --help
cmake --build . //собираем исходник, попадет в папку Debug

# проверка и запуск
 ./Debug/hello_world.exe